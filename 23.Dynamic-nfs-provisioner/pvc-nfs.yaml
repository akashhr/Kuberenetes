apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pv1
spec:
  storageClassName: managed-nfs-storage
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi



# This inturn creates the pv 